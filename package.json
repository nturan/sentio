{
  "name": "sentio-monorepo",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "nx run-many --target=serve --all",
    "build": "nx run-many --target=build --all"
  },
  "private": true,
  "devDependencies": {
    "@nxlv/python": "^22.0.5",
    "nx": "latest"
  },
  "workspaces": [
    "frontend"
  ]
}
